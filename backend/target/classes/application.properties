# Server Configuration
server.port=8080

# Disable database for now (we'll add it later)
# spring.datasource.url=jdbc:postgresql://localhost:5432/scholarstack
# spring.datasource.username=postgres
# spring.datasource.password=postgres
# spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration (disabled for now)
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Security Configuration (we'll add this later)
# jwt.secret=your-secret-key-here
# jwt.expiration=86400000

# Logging
logging.level.com.scholarstack=DEBUG
logging.level.org.springframework.security=DEBUG